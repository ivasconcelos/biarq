'use strict';

describe('myApp.contactos module', function() {

  beforeEach(module('myApp.contactos'));

  describe('contactos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});